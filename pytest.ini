[pytest]
# Offline-only unit suite: no network, no Telegram, no LLM. Deterministic + fast.
# `-p no:seleniumbase` silences an unrelated browser-automation plugin's startup banner.
testpaths = tests
addopts = -q -p no:seleniumbase
